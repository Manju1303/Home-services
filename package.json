{
    "name": "home-services-platform",
    "version": "1.0.0",
    "private": true,
    "description": "Home Services Booking Platform - Full Stack",
    "scripts": {
        "build": "cd backend && npm install && npx prisma generate && npx tsc && cd ../frontend && npm install && npm run build",
        "dev:frontend": "cd frontend && npm run dev",
        "dev:backend": "cd backend && npm run dev"
    }
}